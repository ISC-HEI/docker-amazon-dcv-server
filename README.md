# Trying to run dcvserver in docker

* Dockerfile, a unbutu 24.04 simple docker
* run_in_container.sh what should be done in container
* test.sh launch the containter (it MUST have a fixed mac address).
